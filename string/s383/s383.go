package s383

func RansomNote(note, magazine string) []string {
	// loop over magazine chars and increment magazine[char] += 1
	// Loop over note magazine[Note_char] -=1
	// if negative return false

	return nil
}
